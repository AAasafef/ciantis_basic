import 'package:flutter/material.dart';

import 'universal_grid_menu.dart';

class GridMenu extends StatelessWidget {
  const GridMenu({super.key});

  @override
  Widget build(BuildContext context) {
    return const UniversalGridMenu();
  }
}
